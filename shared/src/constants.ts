/** デフォルトWebSocketポート */
export const DEFAULT_PORT = 8080;

/** プロトコルバージョン */
export const PROTOCOL_VERSION = '1.0.0';

/** 同期除外設定ファイル名 */
export const SYNC_IGNORE_FILE = '.sync-ignore';

/** 一時ファイルの拡張子 */
export const TEMP_FILE_SUFFIX = '.sync-tmp';

/** 再接続の初期待機時間（ms） */
export const RECONNECT_INITIAL_DELAY = 1000;

/** 再接続の最大待機時間（ms） */
export const RECONNECT_MAX_DELAY = 30000;

/** 認証タイムアウト（ms） */
export const AUTH_TIMEOUT = 10000;
